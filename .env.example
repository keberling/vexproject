# ============================================
# VEX Project Management - Environment Variables
# ============================================
# Copy this file to .env and update with your values
# cp .env.example .env
# nano .env

# ============================================
# DATABASE CONFIGURATION (Required)
# ============================================
# SQLite database path (embedded, no installation required)
# For production, consider PostgreSQL or MySQL
DATABASE_URL="file:./prisma/dev.db"

# ============================================
# AUTHENTICATION (Required)
# ============================================
# JWT Secret - Generate a strong random secret for production
# You can generate one with: openssl rand -base64 32
JWT_SECRET="your-secret-key-change-this-in-production"

# NextAuth Secret - Required for JWT signing and session encryption
# Generate a random secret: openssl rand -base64 32
NEXTAUTH_SECRET="your-nextauth-secret-change-this-in-production"

# NextAuth URL Configuration
# For NextAuth v5, AUTH_URL is preferred over NEXTAUTH_URL
# Development:
AUTH_URL="http://localhost:3000"
# Production (update with your domain):
# AUTH_URL="https://yourdomain.com"

# Legacy NextAuth URL (still works, but AUTH_URL is preferred)
NEXTAUTH_URL="http://localhost:3000"
# Production:
# NEXTAUTH_URL="https://yourdomain.com"

# Trust Host - Set to "true" for development, "false" for production
# This allows NextAuth to trust the host header
# Development:
AUTH_TRUST_HOST="true"
# Production:
# AUTH_TRUST_HOST="false"

# ============================================
# MICROSOFT SSO (Optional but Recommended)
# ============================================
# Microsoft Azure AD Configuration
# Required for Microsoft SSO authentication
# See MICROSOFT_SSO_SETUP.md for detailed setup instructions
# Leave empty if not using Microsoft SSO

# Azure AD Application (Client) ID
AZURE_AD_CLIENT_ID=""

# Azure AD Client Secret
AZURE_AD_CLIENT_SECRET=""

# Azure AD Tenant ID
AZURE_AD_TENANT_ID=""

# ============================================
# SHAREPOINT INTEGRATION (Optional)
# ============================================
# SharePoint Configuration
# If left empty, the app will use your default SharePoint site (OneDrive for Business)
# Only set these if you want to use a specific SharePoint site
# See scripts/get-sharepoint-site-id.md for instructions

# SharePoint Site ID (GUID, not URL)
# Leave empty to use default site
SHAREPOINT_SITE_ID=""

# SharePoint Drive ID (usually not needed, leave empty)
SHAREPOINT_DRIVE_ID=""

# ============================================
# GOOGLE MAPS API (Optional)
# ============================================
# Google Maps API Key for address autocomplete
# Get your API key from: https://console.cloud.google.com/
# Leave empty if not using address autocomplete features
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=""

# ============================================
# ADMIN CONFIGURATION (Optional)
# ============================================
# Initial Admin User
# Email of user to set as admin on startup
# The user must exist in the database first (create them by signing up)
# They will automatically be set as admin when the server starts
INITIAL_ADMIN_EMAIL=""

# Admin Backup Secret
# Secret key for scheduled backup API endpoints
# Generate a random secret: openssl rand -base64 32
# Used to secure the scheduled backup webhook
ADMIN_BACKUP_SECRET=""

# ============================================
# SERVER CONFIGURATION (Optional)
# ============================================
# Port number (default: 3000)
# Can be overridden by PORT environment variable or systemd service
PORT=3000

# Node Environment
# Set to "production" for production deployments
# Automatically set by npm scripts, but can be overridden
# NODE_ENV="production"

# ============================================
# NOTES
# ============================================
# 1. Never commit .env file to version control
# 2. Use strong, random secrets in production
# 3. For Microsoft SSO setup, see MICROSOFT_SSO_SETUP.md
# 4. For SharePoint setup, see scripts/get-sharepoint-site-id.md
# 5. Minimum required variables for basic functionality:
#    - DATABASE_URL
#    - JWT_SECRET
#    - NEXTAUTH_SECRET
#    - AUTH_URL or NEXTAUTH_URL
#    - AUTH_TRUST_HOST (for development)
